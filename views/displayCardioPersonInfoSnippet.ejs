<% if(rs.length > 0) { %>
    <% for (var i=0; rs.length > i; i++) { %>
        Day: <%= rs[i].Day %>
        Time: <%= rs[i].Time %>
        Exercise: <%= rs[i].Exercise %><br />
        <% } %>

<% } else { %>
    No Cardio Entries.
<% } %>